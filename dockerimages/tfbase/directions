#Build container by running:
docker build -t helba/tensorflowdockerexample:base .

#To save locally
docker save helba/tensorflowdockerexample:base -o tflowbase.tar

#To load locally
docker load -i tflowbase.tar
