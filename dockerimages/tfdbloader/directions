#This image will pass datasets to our mysql database container
#	for later training.

#Build container by running:
docker build sudo docker build -t localhost:7070/tflow:dbloader .

#To save locally
sudo docker save localhost:7070/tflow:dbloader -o tflowdbloader.tar

#To load locally
sudo docker load -i tflowdbloader.tar
