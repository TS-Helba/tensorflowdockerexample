#Build container by running:
docker build -t helba/tensorflowdockerexample:deps .

#To save locally
docker save helba/tensorflowdockerexample:deps -o tflowdeps.tar

#To load locally
docker load -i tflowdeps.tar
